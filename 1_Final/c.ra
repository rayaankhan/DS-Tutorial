LOAD STUDENTS
LOAD ADVISOR
CROSS_STUDENTS_ADVISOR <- CROSS STUDENTS ADVISOR
PRINT CROSS_STUDENTS_ADVISOR
A <- SELECT advisor_id == adv_id FROM CROSS_STUDENTS_ADVISOR
PRINT A
B <- SELECT stud_sex != adv_sex FROM A
PRINT B
RESULT <- PROJECT stud_id, adv_id, stud_sex, adv_sex FROM B
PRINT RESULT